name: coinbase_api
description: A Dart API Client to test out websockets using Coinbase Cloud
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  json_annotation: ^4.3.0
  json_serializable: ^6.0.1

dev_dependencies:
  build_runner: ^2.1.5
  test: ^1.19.2
  very_good_analysis: ^2.4.0
